# Optional. Install-And-Run.ps1 can also read the token created by `huggingface-cli login`.
HF_TOKEN=

# Optional host output directory. Defaults to ./outputs when unset.
SCENEMA_OUTPUT_DIR=./outputs
